services:
  - type: web
    name: eas-environmental
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn eco_analyze_backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
